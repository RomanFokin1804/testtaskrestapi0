const config = {};

config.database = {
  dbName: 'dbName',
  username: 'username',
  password: 'password',
  dialect: 'mysql',
  host: 'host',
};

config.keys = {
  accessSecret: 'accessSecret',
  refreshSecret: 'refreshSecret',
};

module.exports = config;
